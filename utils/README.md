General `utils` that is not belongs to any specific layer.
For example: util to manipulate strings, numbers, arrays, time ...

Only add new util if **not exists** in the util library: https://github.com/duke-git/lancet